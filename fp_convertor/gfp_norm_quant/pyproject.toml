[project]
name = "gfp_norm_quant"
version = "0.1.0"
description = "GFP11e5 to GFP8e5 normalize and quantize module with cocotb testbench"
requires-python = ">=3.10"
dependencies = [
    "cocotb>=1.9.0",
    "cocotb-tools>=1.9.0",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.0.0",
    "ruff>=0.1.0",
]

[tool.pytest.ini_options]
testpaths = ["sim/cocotb"]
python_files = ["test_*.py"]
python_functions = ["test_*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_ignores = true
